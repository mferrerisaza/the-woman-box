<div class="new-order-wrapper">
  <div class="form-container">
    <%= render "shared/progressbar", active_tab: 3 %>
    <h2 class="form-headline">YA CASI TERMINAMOS, AHORA SOLO NECESITAMOS LOS DATOS DE PAGO Y ENVÍO</h2>
    <p class="form-subheadline">
      Personaliza tu orden seleccionando los tampones o las toallas higiénicas que desees. No te preocupes, podrás cambiar tu selección en cualquier momento.
    </p>
    <%= simple_form_for(@order)  do |f| %>
      <div class="row">
        <div class="col-xs-12 col-sm-6">
          <%= f.input :delivery_date, label: "Fecha de entrega" %>
        </div>
        <div class="col-xs-12 col-sm-6">
          <%= f.input :address, label: "Dirección de entrega" %>
        </div>
        <div class="col-xs-12 col-sm-6">
          <%= f.input :country, label: "País", priority_countries: ["CO"], selected: "CO" %>
        </div>
        <div class="col-xs-12 col-sm-6">
          <%= f.input :province, label: "Departamento / Provincia" %>
        </div>
        <div class="col-xs-12 col-sm-6">
          <%= f.input :city, label: "Ciudad" %>
        </div>
        <div class="col-xs-12 col-sm-6">
          <%= f.input :address_aditional_info, label: "Información adicional", placeholder: "Ej: Torre 1 Apto 302" %>
        </div>
        <%= f.button :submit, "Siguente", class:"btn button banner-call-to-actions button-right" %>
      </div>
    <% end %>
  </div>
  <%= render 'shared/order-description', show_order_box: true %>
</div>
